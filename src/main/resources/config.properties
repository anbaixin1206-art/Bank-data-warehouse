# ============================================================
# Bank Data Warehouse — 配置文件
# 环境变量优先级高于此文件
# ============================================================

# Hive 连接
hive.jdbc.url=jdbc:hive2://localhost:10000/default
hive.user=root
hive.password=

# MySQL 元数据库
mysql.url=jdbc:mysql://localhost:3306/metastore
mysql.user=root
mysql.password=root123

# HDFS
hdfs.uri=hdfs://localhost:9000
stg.path=/data/stg
warehouse.path=/user/hive/warehouse

# Kafka
kafka.brokers=localhost:9092

# Redis
redis.host=localhost
redis.port=6379

# ETL 配置
etl.date=
etl.max.retries=3
etl.retry.interval.sec=120
